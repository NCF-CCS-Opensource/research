# Domain Docs

This is a single-context repository.

Before exploring or changing the application, read:

- `CONTEXT.md` for the canonical domain vocabulary.
- Relevant decisions under `docs/adr/`.

Use glossary terms in issues, tests, code, and documentation. Surface conflicts with an ADR explicitly instead of silently overriding it.
